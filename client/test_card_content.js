import { Card, Table } from '@heroui/react';
console.log('Card keys:', Object.keys(Card || {}));
console.log('Card.Content is:', Card.Content ? 'Defined' : 'UNDEFINED!');
console.log('Table keys:', Object.keys(Table || {}));
console.log('Table.Header is:', Table.Header ? 'Defined' : 'UNDEFINED!');
